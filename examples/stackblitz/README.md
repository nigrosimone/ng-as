# ng-as example

Minimal Angular app using `ng-as` from npm. Run it in the browser:
**https://stackblitz.com/github/nigrosimone/ng-as/tree/main/examples/stackblitz**

Locally: `npm install && npm start`.
